import {Header} from "./Header";
import {Body} from "./Body";

export function LogDisplay() {
    return (
        <div id="LogDisplay">
            <Header/>
            <Body/>
        </div>
    );
}