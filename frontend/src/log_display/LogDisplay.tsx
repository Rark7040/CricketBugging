import {Header} from "./Header";
import {Body} from "./Body";
import {Props} from "../Props";

export function LogDisplay(props: Props) {
    return (
        <div id="LogDisplay">
            <Header/>
            <Body/>
        </div>
    );
}